letters = ["a", "b", "c"]

# This enumeration returns the object where each object contains a tuple containing the index and the value
for index, letter in enumerate(letters):
    print(index, letter)
